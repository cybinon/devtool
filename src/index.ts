import { Theme } from './typesAndInterfaces/interfaces';
import * as utils from './utils'

const dark: Theme = {
    background: '#476646',
    text      : 'test',
    border    : 'skdjaksjdks',
};

export const orgThemes = {
    dark,
};

export {
    utils
}